

function(x){
    if (nclo(x) != 3)
        stop("x must be 3 column")
}
message("success!") # color can turn off
options(message = FALSE)
warning() #you have to pay attention, maybe is wrong

# debug the function
# browse > type n # run the code line by line\
# type c mean tontinue
# type ls() list the all the argument, and check each argument, and print each argument to check



    